import { Variant } from "node-opcua-variant";
import { AddressSpace } from "..";
export declare function resolveOpaqueOnAddressSpace(addressSpace: AddressSpace, variants: (Variant | null) | (Variant | null)[]): Promise<void>;
